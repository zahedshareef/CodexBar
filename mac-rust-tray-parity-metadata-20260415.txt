Fresh macOS parity metadata captured from the live `/Applications/CodexBar.app` process on `mac@imac-ca-mac`.

Capture limitation:
- macOS screen recording from this SSH session only returned wallpaper pixels; `screencapture` and `CGWindowListCreateImage` did not yield usable live window content for CodexBar windows.
- Because of that, the fresh mac proof below uses Accessibility (`System Events`) and WindowServer (`CGWindowListCopyWindowInfo`) metadata instead of fresh pixels, except for the already-captured startup-hidden screenshot.

1 startup hidden
- Fresh screenshot: `~/codexbar-share/captures/mac-rust-tray-parity-startup-hidden-20260415.png`

2 primary tray activation -> summary popup
- A real mouse click on the status-item center (`4259,12`) created a fresh CodexBar layer-101 window:
  - owner: `CodexBar`
  - bounds: `X=4238 Y=25 Width=310 Height=438`
  - alpha: `1.0`
  - onscreen: `true`

3 secondary tray activation -> native menu
- `AXPress` on the CodexBar `AXMenuExtra` exposed the live native menu items:
  - `Buy Credits...`
  - `Add Account...`
  - `System Account`
  - `Usage Dashboard`
  - `Status Page`
  - `Settings...`
  - `About CodexBar`
  - `Quit`

4 Pop Out Dashboard -> normal popout window
- Clicking the live `Usage Dashboard` menu item opened an unnamed CodexBar window with:
  - position: `[5119,25]`
  - size: `[1200,1600]`

5 provider-specific tray popout -> selected provider detail
- Clicking the live `System Account` menu item opened an unnamed CodexBar window with:
  - position: `[5119,25]`
  - size: `[1200,1600]`

6 closing main window hides to tray instead of exiting
- After opening the provider/detail window, sending `Cmd-W` left only the keepalive window in Accessibility:
  - `CodexBarLifecycleKeepalive` at `[-5000,7879]` size `[20,20]`
- The main CodexBar process remained alive.
- The status item still exposed `description=status menu`.

7 popup/popout placement stays on correct monitor/work area
- Fresh popup bounds: `4238,25 310x438`
- Fresh dashboard/detail bounds: `5119,25 1200x1600`

8 Settings stays tray-first instead of making main window primary
- Opening `CodexBar > Settings...` created a fresh `General` window with:
  - position: `[563,407]`
  - size: `[496,660]`
- The status-item WindowServer entry remained present while Settings was open.

9 About uses official icon + canonical branding copy
- Opening `CodexBar > About CodexBar` created a fresh unnamed About window with:
  - position: `[2418,646]`
  - size: `[284,187]`
- Because SSH screen recording hid live window pixels, the last visual branding reference remains `~/codexbar-share/captures/mac-settings-front.png`.
